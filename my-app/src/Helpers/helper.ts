import internal from "stream";

export  function x(apps: number){
    apps = 1;
    return 1;
}
enum Types {
    Truck,
    Pickup,
    Car
}
export const hello = (x: Types) =>{
    return x*2;
}
const hap = 3;
hello(hap);

const myObj = {
    foo:'s'
}
Number.isNaN(myObj);

